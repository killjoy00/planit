name: Dependency lock refresh

on:
  push:
    branches:
      - codex/dependency-security-cleanup

permissions:
  contents: write

concurrency:
  group: dependency-refresh-final
  cancel-in-progress: true

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/dependency-security-cleanup
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Generate verified dependency lockfile
        run: |
          npm install --package-lock-only --ignore-scripts
          npm audit fix --package-lock-only --ignore-scripts
          npm audit --audit-level=low
      - name: Commit generated lockfile
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Lock audited dependency updates [skip ci]"
          git push origin HEAD:codex/dependency-security-cleanup
